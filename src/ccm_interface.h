#include "forecast_machine.h"
#include "xmap.h"

class ccm_interface
{
    ccm_interface();

    // list block
    // double lib = c(1, NROW(block));
    // double pred = lib;
    // double norm = 2;
    // int E = 1;
    // double tau = 1
    // double tp = 0;
    // char num_neighbors = "e+1";
    // int lib_sizes = seq(10, 100, by = 10);
    // bool random_libs = TRUE;
    // double num_samples = 100;
    // bool replace = TRUE;
    // double lib_column = 1;
    // int target_column = 2;
    // bool first_column_time = FALSE;
    // bool RNGseed = NULL;
    // double exclusion_radius = NULL;
    // bool epsilon = NULL;
    // bool silent = FALSE;
};